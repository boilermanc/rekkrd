name: Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          port: 2222
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            set -e
            cd ~/app
            git pull origin main
            npm install --legacy-peer-deps
            npm run build
            cp -r dist/* ~/httpdocs/
            pm2 restart rekkrd-api
